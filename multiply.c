#include<stdio.h>
void main()
{
int i,a[i];
int x=1,N;
printf("Total numbers:");
  scanf("%d",&N);
    printf("Enter the numbers:");
  for(i=0;i<N;i++)
    scanf("%d",&a[i]);
  for(i=0;i<5;i++)
  {
    x=x*a[i];
  }
  printf("ans:%d",x);
}
