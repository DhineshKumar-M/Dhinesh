#include<stdio.h>
int main()
{
int i,a[i],x;
printf("enter the integers");
for(i=0;i<10;i++)
scanf("%d",&a[i]);
{
  x=a[0];
  for(i=0;i<10;i++)
  {
      if(a[i]>x)
      x=a[i];
  }
    printf("max=%d",x);
}
}
